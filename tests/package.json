{
  "name": "booksearch-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.priority.config.ts",
    "test:headed": "playwright test --config=playwright.priority.config.ts --headed",
    "report": "playwright show-report",
    "test:live": "playwright test --config=playwright.config.ts",
    "measure:featured": "node measure-featured.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
