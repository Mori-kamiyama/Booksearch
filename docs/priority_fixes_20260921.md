# 優先度の高い小規模修正 — 実装とレビュー

日付: 2026-09-21

第1・第2弾は当時の記録。最新の実装範囲・検証・残件は末尾の第3弾を参照。

## 対象と作業場所

最新 `origin/master` (`2b758a8645a83553833789a3da60bebc2d37c2ff`) から作成した隔離worktreeで実装。

- ブランチ: `codex/booksearch-priority-fixes`
- 作業場所: `/Users/yuta/.codex/worktrees/booksearch-priority-fixes/Booksearch`
- Luna 3エージェントが検索、ジョブ結果、棚/索引をファイル単位で分担。
- 元のcheckoutは14コミット古く未追跡の資料等があるため、アプリの修正はそこへ直接上書きしていない。

## 実装範囲

| 既存項目 | 今回の修正 | 残る範囲 |
|---|---|---|
| #3 | 古い検索の成功・失敗が新しい結果を上書きしない。空クエリ・離脱も無効化 | 共通タイムアウト等は別作業 |
| S04 | 320〜1024pxの検索幅を修正。768/820pxでは4列、1024px以上は5列。書影も列幅に収める | 全画面のレスポンシブ対応は未完 |
| S08 | 検索の人為的な350ms待機を削除 | 他画面のレイアウト変化等は未完 |
| A02 / #4 | 初回取得失敗に再試行。uploading・未知状態・未検出・読取不可を成功と区別。ジョブ切替の古い応答を破棄し、ポーリング重複を回避 | スキャン自体の開始/停止状態は未変更 |
| A03の一部 | 棚未判定entryから架空の1棚を加算しない | 曖昧な蔵書候補と確定成果の分離は未着手 |
| A04 | 棚IDの二重デコードを削除し、実在棚で検証。無効IDは地図へ戻す | アプリ全体の例外境界・汎用404は未着手 |
| A13 / #3 | マップと蔵書索引のデータ・loading・error・retryを独立 | 索引のページングや選択棚の復元は未着手 |
| A19の一部 | 上記の本番モードの回帰テストを追加 | 既存のライブE2E全体の更新は未着手 |

## 検証

- `frontend`: `npm ci` でlockfileから依存をインストール。
- `frontend`: `npm run build` 成功。
- `frontend`: `npm test` — 5ファイル、22テスト成功。
- 本番ビルドをローカルpreviewし、`tests`: `npx playwright test --config=playwright.priority.config.ts` — 23テスト成功。
- `git diff --check` 成功。

回帰テストは `tests/ui-regression/priority-fixes.spec.ts` に配置。既存の `tests/e2e` は本番向け設定を含むため、制御した応答を使う今回のテストを独立configで分離した。API・外部画像は遮断/差し替えし、本番データの書き込みやスキャンは行わない。

主な確認: 応答順序逆転、古い500応答、空検索への戻る、320/375/768/820/1024pxでのはみ出し・表紙重なり、初回404からの再試行、4種類の非成功状態、棚未判定件数、不正/実在棚ID、索引の片側失敗と復帰、ジョブID切替、棚切替後の遅延失敗、Map/Listの二重再試行。

## レビューでの調整

- 親エージェントが全4ページの差分を確認し、320px幅で書影がグリッド列より広くなる点を修正依頼。max-w-full等で列内に収め、隣接書影が重ならないテストを追加した。
- ジョブエラー状態のnull可能性を型チェックで確認し、明示的なnullガードへ修正した。
- 単に「成功文言がない」だけでは初期Loadingでテストが通るため、状態の見出しが描画されてから非成功表示を確認するようテストを修正した。
- 検索担当が別ファイル（ジョブ・棚・索引）を独立レビューし、棚切替後の古い失敗、索引の二重再試行による古い失敗の2点を指摘した。親が3つの回帰テスト（棚、Map、List）で修正前の失敗を再現し、担当Lunaがリクエスト世代管理を追加した。23テストの最終実行で全件成功し、独立レビューでも両指摘の解消を確認した。今回の変更範囲で未解決のレビュー指摘はない。

## 制約と未実施

実カメラ、実OCR、本番API、Safari/実iPhoneでの動作は未確認。既存ライブE2E一式は今回実行していない。検索APIの通信自体は中断せず、古い応答の反映を抑える方式。ジョブ通信にはAbortControllerを使用。

既存依存のインストール時にnpm auditはfrontendで9件（low 1 / moderate 4 / high 4）を報告した。依存更新は本修正に混ぜていない。既知のOpenCV大容量チャンクとビルド警告も残る。

第1弾時点ではコミット・push・デプロイは未実施。


## 第2弾: スキャン後の導線と小さなUX修正

第1弾と同じworktree・ブランチに追加。LunaがJobPageとScanPageを分担し、親が書影の引き継ぎと回帰テストを担当。別のLunaが追加差分を読み取りレビューした。

| 項目 | 実装したこと | 残る範囲 |
|---|---|---|
| A10 | スキャン結果の自動照合済み蔵書から詳細へリンク。検索・再スキャンへの終了後導線 | 探索対象本/棚の引き継ぎ・対象本優先表示はF01/A08で扱う |
| A03の表示部分 | review・確度不明・不正IDは「照合候補・要確認」、候補なしは「未照合」。確定リンクと区別 | 撮影中のポップアップ、総数の意味、保存完了との区別は未着手 |
| A09 | ファイル選択後に「カメラに戻る（選択解除）」を追加。同じファイルを選び直せる | 実カメラの端末別動作は未確認 |
| A17の選択ボタン | 白い無地ボタンをアイコン＋「選択」へ変更。送信/停止/カメラ起動中は選択・切替を無効化 | ファイル形式・サイズ制限と動画プレビューは未着手 |
| A16の書影引き継ぎ | 棚候補から渡されたthumbnailを棚詳細のカードへ保持 | 画像404時の共通フォールバックは未着手 |

照合判定は既存バックエンドの `match_confidence === 'auto'` と正の整数 `library_db_id` の両方を使う。新しいスコア閾値は作っていない。autoは既存モデルの自動採用であり、人が正しさを保証した意味ではない。`known_books`のみで蔵書IDがない候補はリンクしない。同一棚・同一蔵書IDの後続autoは先行reviewを置き換える。

親レビューで、IDなし候補をタイトル一致だけで後続のID付き蔵書へ統合する処理を除外した。同名本や版違いを根拠なく同一視しないため。同一ID内の昇格は維持する。カメラへ戻る操作は権限要求や撮影開始を自動では行わず、プレビューURLの既存cleanupも維持する。独立レビューで追加の不具合指摘はなかった。

最終検証:

- 本番ビルド成功、単体テスト22件成功、Chromium回帰テスト29件成功。
- 新規6ケース: 確定候補のみ詳細リンク＋終了後導線、review→autoの同一ID昇格、ファイル→カメラ→同一ファイル再選択（権限要求0回）、送信中の選択禁止と失敗後の解除、棚詳細の書影保持、ID不明の同名本を統合しないこと。
- 第1弾23ケースも通過。実API・実カメラ・Safariでの確認は未実施。未コミット・未push・未デプロイを継続する。

## 第3弾: 既存機能の安定化をまとめて適用

ユーザー指定により、既存機能の不具合・小さな操作改善を対象とし、新しい探索導線・AI・絞り込みは後回しとした。今回の最後にcommit/pushする。本番への反映は別作業。

- 検索: 複数語AND、LIKEワイルドカードのリテラル化、記号だけの全件化防止、ISBN表記正規化、安定した順位、総数と表示件数の区別。
- 棚: AWS候補のページ読み切り、候補重複による冊数水増しを抑制、最新観測を高確度の一冊から独立させる。最新観測日は棚全体の確認を意味しないと明記。
- おすすめ: 仮IDを除去、週内で安定した実蔵書選定、通信の集約とキャッシュ、失敗時の再試行。AI生成・永続的な週次配信基盤は追加しない。
- 詳細: 遅い旧リクエストで別の本に戻る競合を抑制。未取得の読了時間を3時間と表示しない。通常の説明文をAI要約と呼ばない。
- 共通: 入力ラベルと検索ボタン、IME、メニューのフォーカス、未知URL・例外からの復帰、表紙読み込み失敗時の代替表示、ページタイトル。
- 索引: 最初の描画を120冊に制限し、同じ索引の続きは120冊ずつ表示する。API上は全蔵書を保持して総数を表示する。
- スキャン: 開始の多重実行防止、セッションごとの非同期処理管理、停止時のエンコード/送信待ち、確定失敗時の再試行、タグ検知と本の登録の区別、ファイル検証、背景時の処理抑制。
- テスト: 通常のtests/npm testはローカルpreview＋模擬API。旧ライブテストは現在のUIに合わせた読み取り専用smokeへ整理し、URL明示時のみ実行する。

### 後で扱う範囲

F01–F05、L01/L02/L04、A08/A14、AI検索・AI要約・関連本推薦品質は保留。検索のページ送り、探索状態の完全復元、詳細の位置情報を主役にする再配置、物理マップ全体の操作設計も次の導線検討で扱う。実機性能、OCR精度、実登録から検索反映までの本番測定、週次おすすめのp95計測はこのローカル検証だけで完了とは言わない。GitHubの大きなIssue #3–#5も受け入れ条件全体を満たすと判断するまではcloseしない。

### 第3弾のレビュー・検証

親と担当外のLunaで差分を確認し、以下を追加修正した。

- Responseヘッダー受信時にタイムアウトを解除するとbodyの読み込みが無限待ちになるため、body消費中も中断signalを有効にした。
- 旧ファイル送信が離脱後に完了して別画面からジョブへ遷移する競合を、XHR中断と世代チェックで抑制した。
- 失敗したフレームがpending集合から消えた後でも、セッションに失敗を保持して確定を止める。撮り直しが必要な場合はキャンセルを促す。
- `toBlob`の未完了callbackを停止処理が待つこと、送信・commit後にのみ確定することを模擬カメラで確認した。
- 静止フレームの基準画像を最後の送信候補に変更した。各評価で更新すると、タグなし初回送信前に「変化なし」となり、一度も送れないため。
- 検索後すぐ戻る操作でURLと入力がずれる回帰を再現し、同じクエリへ戻る場合にもlocation変更で同期するよう修正した。

検証（第3弾、ローカル）:

- frontend: `npm run build` 成功、`npm test` 26件成功。
- tests: `npm test` Chromium 43件成功。実APIを遮断したproduction preview上で実行。
- backend / aws/functions/go_api: 各 `go test ./...` 成功。
- `git diff --check` 成功。
- 旧ライブsmokeは `FRONTEND_URL=http://127.0.0.1:4179 playwright test --config=playwright.config.ts --list` で収集12件を確認。ライブ実行はしていない。

ブラウザ43件には旧23件、第2弾6件に加え、空/失敗おすすめ、短画面ホーム、詳細の遅延競合、最新観測、索引分割、検索総数、IME/フォーカス、未知URL、模擬カメラ二重開始と再確定、直接URLの戻る、画像404、送信中離脱、停止時エンコード待ち、フレーム失敗時の確定抑止を含む。

OpenCVの既知の大容量チャンク警告は残る。実カメラ/Safari/実OCR/本番レイテンシ/本番登録結果は未検証であり、これらのテスト成功をデプロイ確認とは扱わない。

サーバー側レビューで、キュー送信とDB更新の二重障害、workerとのキャンセル競合に関する耐久性の残件も見つかった。APIの完了再試行・cancel前の状態確定・complete/cancel競合時の409は今回修正する。分散処理全体のdurable retry/outboxは今回導入せず、A22に根拠と未検証範囲を記録した。

サーバー側の最終追加確認:

- AWS SDKの模擬サーバーで500件＋次ページ、途中ページ失敗、processing完了再試行、done再実行、確定済みcancelでS3削除なし、complete/cancel競合で成功としないことを確認。
- ローカルsession操作はmutexで直列化し、同時completeが同じjob IDを返す。保存済みjob IDで応答喪失後の再試行も復旧する。
- おすすめはStore内のUTC ISO週キーのキャッシュとHTTP/クライアントキャッシュを使用。初回取得後DBを閉じても同週の再取得ができるテストを追加。週更新失敗時の永続的な旧週リスト維持は未実装。
- 両Goモジュールで `go test -race ./...` を実行。

検索即戻りは、途中の検索画面をReactがcommitする前にBackすると、最終URL/keyが以前と同じになり、URL依存effectだけでは編集済みdraftを戻せないことが原因だった。React stateとURLを一時的に比較して確認し、popstateでもURLから入力draftを同期する修正に更新した。対象ケース12回連続成功を確認。通常の `tests/npm test` はbuild/previewも自動起動し、手動でサーバーを準備せず再現できる。
